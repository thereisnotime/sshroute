// Package config — validator checks the config structure for correctness.
package config

// Validate returns an error if the config is invalid.
func Validate(cfg *Config) error { return nil } // implemented by A1
